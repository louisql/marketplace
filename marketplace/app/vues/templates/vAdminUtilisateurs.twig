{# {{oUtilisateur.utilisateur_prenom  | json_encode(constant('JSON_PRETTY_PRINT')) }}
{{oUtilisateur.utilisateur_id  | json_encode(constant('JSON_PRETTY_PRINT')) }} #}


<script>history.pushState({}, '', 'admin?entite=utilisateur')</script>
<section>
  <h1>{{ titre|upper }}</h1>
  <a href="admin?entite=utilisateur&action=a">Ajouter un utilisateur</a>
  <p class="messageRetourAction {{ classRetour }}">{{ messageRetourAction|raw}}&nbsp;</p>   
  <div class="g utilisateurs">
    <div class="t">Id</div>
    <div class="t">Nom, Prénom
</div>
    <div class="t">Profil</div>
    <div class="t">Courriel</div>    
    <div class="t">Actions</div>
  {% for utilisateur in utilisateurs %}
    <div class="l">
      <div class="c dr">{{ utilisateur.idUtilisateur }}</div>
      <div class="c   ">{{ utilisateur.nomUtilisateur }}, {{ utilisateur.prenomUtilisateur }}</div>
      <div class="c dr  ">{{ utilisateur.nomRole }}</div>
      <div class="c ">{{ utilisateur.courrielUtilisateur }}</div>
      <div class="c   ">
        <a href="admin?entite=utilisateur&action=m&utilisateur_id={{ utilisateur.idUtilisateur }}">modifier</a>
        {# {% if not auteur.nb_livres %} #}
        <span class="confirmer" data-href="admin?entite=utilisateur&action=s&utilisateur_id={{ utilisateur.idUtilisateur }}">supprimer</span>
        {# {% endif %} #}
        {# <a href="admin?entite=utilisateur&action=generer_mdp&utilisateur_id={{ utilisateur.idUtilisateur }}">générer mdp</a> #}
      </div>
    </div>
  {% endfor %} 
  </div>
</section>
{{ include("modaleSuppression.twig") }}

